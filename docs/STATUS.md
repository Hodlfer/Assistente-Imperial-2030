# Status do projeto — Assistente Imperial 2030

Acompanhamento das sessões de desenvolvimento. Cada sessão deve atualizar
esta tabela ao terminar.

| Sessão | Descrição | Status | Notas para a próxima sessão |
|---|---|---|---|
| 0 | Setup do projeto (scaffold, docs, deploy no Pages) | ✅ Feito | Scaffold Vite+React+TS+Tailwind v4+Vitest+ESLint pronto. Estrutura de pastas criada (`src/engine`, `src/components`, `src/hooks`, `src/data`). Workflow de deploy no GitHub Pages criado em `.github/workflows/deploy.yml` (falha se `npm test` falhar) — falta confirmar com o usuário que o Pages foi habilitado em Settings → Pages (Source: GitHub Actions) e que o primeiro deploy publicou com sucesso. `docs/REGRAS.md` foi criado com os dados de referência fornecidos, mas a tabela de obrigações (valor→juros) ainda precisa ser **validada contra as cartas físicas** antes da Sessão 1 implementar o Investidor. |
| 1 | Engine puro do jogo (`src/engine/`) | ✅ Feito | Engine puro em `src/engine/` (tipos, estado/transações, undo, governo, tributação, juros, pontuação) + constantes em `src/data/regras.ts`. 21 testes verdes cobrindo os cenários A–G, C2 e invariantes. **Notas para a UI:** (1) Estado é event-sourced — nunca editar direto; usar `aplicarTransacao`/`comprarObrigacao`/`trocarObrigacao`/`aplicarTributacao`/`pagarJuros`/`recalcularGovernos`, que retornam NOVO estado (imutável). `desfazer(estado)` faz o undo. (2) `governanteId` fica guardado em `EstadoNacao` e só muda ao chamar `recalcularGovernos` (fim do Investidor); comprar/trocar obrigação NÃO recalcula sozinho. (3) Toda transação carrega `resultado` com os fluxos calculados — a UI mostra essa prévia antes de confirmar. (4) Juros podem deixar o governante com dinheiro negativo (regra E). (5) `criarEstado(jogadores, nacoesParciais?)` monta o estado e a pilha de obrigações disponíveis automaticamente. ⚠️ Ainda depende da validação física da linha valor→juros das obrigações antes do fluxo completo do Investidor. |
| 2 | Wizard de início de partida + `useGame` | ✅ Feito | `src/hooks/useGame.ts`: hook em memória sobre o engine — `estado`, `erro` (mensagem de invariante violado, para toast), `dispatch((estado) => Estado)` com try/catch, `desfazer()`, `iniciarPartida(jogadores, nacoesParciais)`. Também exporta `estadoInicialComGovernos`, que monta um estado provisório, aplica `recalcularGovernos` e usa o resultado como snapshot de uma NOVA `PartidaIniciada` — assim o baseline do undo já nasce com governos/Bancos Suíços corretos numa única transação; é usada tanto pela prévia do Resumo quanto por `iniciarPartida`. Wizard em `src/components/setup/` (5 etapas: Jogadores, Capital, Obrigações, Investidor, Resumo — orquestradas por `SetupWizard.tsx`, modelo/helpers puros em `modelo.ts`). Etapa de Obrigações usa abas por jogador + grade 6×9 tocável, com validação de sobra=2 não-bloqueante (aviso com confirmação). Etapa de Investidor sugere o portador (calcula localmente quem controla Rússia/China pela soma de valores, sem depender do engine) e pré-seleciona, mas a escolha continua manual. Tokens de cor das nações em `src/data/cores.ts` (classes Tailwind literais, para o scanner do Tailwind v4 detectar). `App.tsx` liga tudo: mostra o wizard enquanto `useGame().estado` é `null`. 23 testes verdes, incluindo um teste de integração (`SetupWizard.test.tsx`) que simula o critério de aceite (4 jogadores) e confere os 6 governos calculados. **Notas para a próxima sessão:** (1) O tabuleiro/partida em andamento ainda não existe — `App.tsx` só mostra um placeholder após "Iniciar partida"; (2) a UI de obrigações não impede duplicar visualmente uma nação sem tesouro/PP ainda, isso só existe a partir da Sessão 3+; (3) `useGame.dispatch` espera uma função `(estado) => Estado` (ex.: `dispatch((e) => comprarObrigacao(e, ...))`), não uma `Transacao` bruta — mais simples de compor com os helpers do engine. |
| 3 | Dashboard principal (`src/components/dashboard/`) | ✅ Feito | Tela que fica aberta durante a partida inteira, sem ações do rondel ainda. **Faixa de nações** (`FaixaNacoes`/`CartaoNacao`): cartão por nação na cor dela com tesouro, PP + Fator de Poder ("17 PP · x3"), governante ("—" com "pula o turno" se null) e a pilha de 9 obrigações compacta (vendidas riscadas), mais a barra de progresso da nação líder rumo a 25 PP com aviso "Fim de jogo próximo" a partir de 20. **Cartões de jogadores** (`CartoesJogadores`/`CartaoJogador`): dinheiro com toggle de ocultar por jogador (padrão visível, estado local por cartão — não é global), obrigações agrupadas por nação (soma de valores + soma de juros), badges 🏦/💼, e uma faixa colorida no topo do cartão para cada nação que o jogador governa. **Aviso de controle latente**: `modelo.ts` expõe `controleLatente(estado, nacao)`, que compara a soma de obrigações do governante atual com a de cada outro jogador e sinaliza (ícone ⚠ discreto no cartão da nação, com tooltip) quando alguém já supera — sem tocar em `governanteId` (só o Investidor recalcula isso). **Correção manual**: nova transação de engine `AjusteManual` (`src/engine/types.ts` + `estado.ts`, exportada via `aplicarAjusteManual`) — +/− em tesouro de nação ou dinheiro de jogador, ou mover uma obrigação entre jogador/pilha disponível, com motivo obrigatório; passa pelo pipeline normal (histórico + undo). UI em `AjusteManual.tsx`, aberta por um botão no cabeçalho. **Botão flutuante "Ações"** (`AcoesFlutuante.tsx`): menu com as ações do rondel citadas em `docs/REGRAS.md` mais as clássicas de Imperial ainda não detalhadas (Produção, Manobra, Importação, Investidor, Tributação, Fábrica), todas desabilitadas — implementação real e ordem exata do rondel ficam para a Sessão 4. **`useGame`** ganhou `carregarEstado(estado)`, que substitui o estado atual sem recalcular governos — usado hoje só pelo fixture de dev, mas pensado para o import de JSON da Sessão 5. **Fixture de dev**: `src/dev/fixtureDashboard.ts` (6 jogadores, ~22 obrigações distribuídas, dois nomes longos, uma nação órfã e três avisos de controle latente incluindo o caso órfão) carregável por um botão "Carregar exemplo (dev)" em `App.tsx`, visível só em `import.meta.env.DEV`. Testado visualmente em viewport de tablet (1024×768, screenshots via Playwright): tudo legível sem rolagem, nomes longos truncam com reticências em vez de quebrar o layout. 33 novos testes (engine + `modelo.ts` do dashboard + `Dashboard.test.tsx` de integração), 42 no total, `npm test`/`tsc -b`/`eslint` verdes. **Notas para a Sessão 4:** (1) as ações do rondel na `AcoesFlutuante` são só placeholders com nomes — a ordem real do rondel, os custos por espaço extra (1 + Fator de Poder) e os efeitos de cada ação ainda não foram implementados nem auditados contra o manual; (2) `AjusteManual` não valida se o alvo de "mover obrigação" faz sentido colocando duas obrigações de mesmo valor na mesma pilha (a validação de duplicata já existe no engine — comprar/trocar checam `obrigacoesDisponiveis.includes`, mas mover para a pilha via ajuste manual não impede duplicar um valor que já esteja lá; não surgiu como problema real porque o fluxo normal do jogo nunca duplica, mas vale revisar se aparecer um caso de uso). |
| 4 | Ações do rondel com preview e confirmação | ✅ Feito | Toda ação segue "selecionar nação ativa → inputs → preview dos fluxos → confirmar" e dispara **UMA transação composta desfazível de uma vez**. **Engine:** nova primitiva `MovimentoBanco` (débito/crédito contra o banco), `CartaInvestidorPassada`, e a composta `AcaoRondel` (guarda `passos: Transacao[]` e a `nacao` ativa); `aplicarEfeito` foi refatorado para aplicar efeitos in-place e a composta reaplica cada passo em sequência (replay-safe). `GovernosRecalculados` ganhou `portadorId?` opcional: com ele, o desempate entre novos empatados usa a ordem horária a partir do portador da carta (mantém o atual se ele seguir empatado); sem ele, mantém o comportamento antigo (empate mantém o governo — recálculo inicial da partida). Construtores puros em `src/engine/acoes.ts` (`construirFabrica/Importacao/Producao/Manobra/Tributacao/EspacosExtras/InvestidorParar/InvestidorPassar`) montam a `AcaoRondel` encadeando os helpers existentes sobre um estado espelho e fatiando as transações geradas — o preview lê os `resultado` já calculados. Também: `nacaoAtivaSugerida` (cursor de turno = próxima nação após a última `AcaoRondel`), `custoEspacosExtras` (n × (1 + Fator de Poder)), `ordemBancosSuicos` (horária a partir da esquerda do portador), seletores `jurosDaAcao`/`tributacaoDaAcao`/`governosTrocados` para o preview. `NOMES_NACAO` centralizado em `src/data/regras.ts`. **UI:** `AcoesFlutuante` agora abre um menu ativo (Fábrica, Produção, Importação, Manobra, Tributação, Investidor parar/passar, Espaços extras) + "Desfazer última ação"; cada ação abre um modal (`src/components/dashboard/acoes/`): `AcaoSimplesModal` (fábrica/importação/produção/manobra/espaços extras), `TributacaoModal` (4 etapas com casos de tesouro insuficiente e gatilho de fim de jogo≥25 PP → `FimDeJogoModal` placeholder da Sessão 6), `InvestidorModal` (3 etapas numa tela com **simulação incremental** para dinheiro/opções corretos a cada investidor, rateio dos juros com insuficiência, seletor de compra/upgrade só com trocas válidas, e resumo de governos trocados + carta passada). **Testes:** 11 novos (9 engine em `acoes.test.ts` incluindo o Investidor ponta a ponta — juros insuficientes + upgrade + Banco Suíço + troca de governo + passagem da carta numa transação só — e "passar por cima não paga juros"; 2 de UI em `acoes/Acoes.test.tsx` reproduzindo os cenários C (tributação EUA) e B (upgrade China) com os previews corretos). 53 testes verdes, `tsc -b`/`eslint`/`build` limpos; fluxo Investidor→Tributação validado no navegador (tablet 1024×768, sem erros de console). **Notas para a próxima sessão:** (1) o cursor de turno é derivado do histórico e usado só como default do seletor (editável), não pula nações órfãs automaticamente; (2) `MovimentoBanco` não bloqueia tesouro/dinheiro negativo (fábrica/importação/espaços extras podem deixar saldo negativo, coerente com a regra E dos juros) — a UI mostra o resultado mas não impede; (3) fim de jogo é só placeholder até a Sessão 6 (pontuação); (4) a passagem da carta e o desempate horário usam `assento` — conferir que o wizard sempre define assentos consistentes. |
| 5 | Histórico, undo/redo e persistência | ✅ Feito | Uma partida real dura ~120 turnos/horas — esta sessão torna o app confiável para isso. **Redo:** `useGame` (`src/hooks/useGame.ts`) foi refatorado para um único `useState` com `{ estado, pilhaRefazer }` (evita duas chamadas de `setState` interdependentes); expõe `refazer()`, `podeDesfazer`, `podeRefazer`. `dispatch`/`carregarEstado`/`iniciarPartida` zeram a pilha de refazer; `desfazer()` empilha a transação removida, `refazer()` a reaplica via `aplicarTransacao` (replay-safe, já que toda `AcaoRondel` reaplica seus passos). **Histórico** (`src/components/historico/`): `modelo.ts` é puro — `construirHistorico(estado)` reconstrói o estado após cada transação (fold incremental com `aplicarTransacao`) e formata cada linha com um título por tipo (`tituloDe`) e badges de fluxo (`flagsDe`/`flagsTributacao`/`flagsJuros`/`flagsGovernos`/`flagsAcaoRondel`) — o formato bate exatamente com o exemplo do enunciado ("Tributação — EUA · +11 tesouro · −3 salários · bônus 2 → Ana · +4 PP (12→16, x3)", coberto por teste). `detalhesDaTransacao` expande os fluxos individuais sob demanda (só quando a linha é tocada), inclusive passo a passo de dentro de uma `AcaoRondel`. `PainelHistorico.tsx` é a gaveta lateral (linhas em ordem reversa, ajuste manual destacado em âmbar com o motivo); `ConfirmarDesfazer.tsx` mostra o resumo da transação antes de desfazer (reutiliza `AcaoModal` da Sessão 4). **Persistência** (`src/persistence/`, lib `idb`): `db.ts` (IndexedDB, uma partida por dispositivo, chave fixa `'atual'`), `localStorageMirror.ts` (espelho leve, best-effort, nunca lança), `storagePersist.ts` (`navigator.storage.persist()` uma vez por sessão da página), `exportImport.ts` (export baixa/copia JSON; import **reprocessa cada transação pela engine** do zero via `aplicarTransacao` — não confia no `jogadores`/`nacoes` do arquivo — e aborta reportando qual transação falhou, sem tocar no estado atual). `src/hooks/usePersistencia.ts` envolve `useGame`: autosave assíncrono a cada transação (IndexedDB + espelho, non-blocking via `useEffect`), aviso persistente (`AvisoSalvamento`) se a gravação falhar, carrega o save ao montar (fallback pro espelho se o IndexedDB voltar vazio), oferece `saveBrutoIncompativel` (JSON cru para download) quando `schemaVersion` é desconhecida em vez de tentar reconstruir e quebrar (`TelaMigracao.tsx`). `App.tsx` decide entre tela de carregamento → `TelaMigracao` → `TelaContinuar` ("Continuar partida de [data] ([n] transações)" vs "Nova partida", com confirmação antes de descartar) → wizard (com atalho de importar um JSON) → `Dashboard`. `ExportImportModal.tsx` é compartilhado entre a tela inicial (só importar) e o cabeçalho do Dashboard (exportar+importar, pede confirmação antes de sobrescrever a partida em curso). **Testes:** 67 no total (14 novos: 4 de `historico/modelo.test.ts`, 10 de `persistence/persistence.test.ts` com `fake-indexeddb` — round-trip export/import deep-equal, serializar/desserializar preserva o resultado de `desfazer`, fallback do IndexedDB "vazio" para o espelho, nome de arquivo, e mensagens de erro de import). `fake-indexeddb/auto` + um `beforeEach` global em `src/setupTests.ts` (limpa IndexedDB/espelho antes de cada teste) foram necessários porque jsdom não tem IndexedDB nativo e o save de um teste "vazava" pro próximo dentro do mesmo arquivo. **Critério de aceite validado de ponta a ponta** com Playwright headless usando um `userDataDir` persistente (fechar/reabrir o "navegador" de verdade, não só a aba): carregou a fixture de dev, aplicou um ajuste manual, fechou o contexto, reabriu apontando pro mesmo perfil → tela "Continuar partida" apareceu com a contagem certa de transações, e o histórico/ajuste sobreviveram; desfazer/refazer testados na sequência. **Notas para a próxima sessão:** (1) `playwright` foi usado só para essa verificação manual e foi removido do `package.json` depois — reinstalar (`npm install -D playwright`) se for reusar o mesmo script de fechar/reabrir; (2) `SCHEMA_VERSION` está em `src/persistence/tipos.ts` — bump manual + escrever uma migração de verdade (hoje só oferece export do JSON bruto) no dia em que `Estado` mudar de um jeito que quebre saves antigos; (3) o botão "Exportar/Importar" e o painel de histórico ainda não foram auditados em viewport de tablet (1024×768) como as sessões anteriores fizeram — vale um passe visual antes da Sessão 6; (4) fim de jogo (pontuação) continua placeholder, aguardando a Sessão 6. |
| 6 | Fim de jogo e pontuação final | ✅ Feito | **Engine (`src/engine/pontuacao.ts`):** `jogoTerminou(estado)`/`nacoesEncerrando(estado)` derivam o fim de jogo do estado (alguma nação ≥ 25 PP) — então undo/redo de uma tributação refletem no fim automaticamente. `detalharPontuacaoJogador` monta o detalhamento por obrigação (`juros × Fator = pontos`, ex. Índia 12 → 5 × 3 = 15), subtotal e total; obrigações ordenadas por ordem de turno + valor decrescente. `classificacaoFinal` ordena os jogadores, resolve desempates (maior soma de obrigações na nação com mais PP; persiste → próxima nação em PP decrescente) e detecta **empate real** (mesmo total e mesmas somas nas 6 nações → colocação compartilhada), expondo em cada entrada o `desempate` (critério `{nacao, pontosPoder}` que a colocou à frente do próximo) e `empateReal`. `pontuacaoFinal` reimplementado sobre `classificacaoFinal` (compat). **Caso especial do manual (`src/engine/acoes.ts`):** `construirInvestidor` retorna uma `AcaoRondel` de passos vazios quando o jogo já terminou **e** a nação apenas PULOU o Investidor (só `!comJuros`) — etapas 2–3 (investimentos) e a passagem da carta não acontecem; os juros da etapa 1 já não ocorriam ao passar. "Parar no espaço" segue normal mesmo com o jogo encerrado. **UI:** `Dashboard` deriva `terminou = jogoTerminou(estado)` e abre `PlacarFinal.tsx` automaticamente (padrão de ajuste de estado no render, sem `useEffect`, para reabrir num novo encerramento após undo); passa `bloqueado` a `AcoesFlutuante` (esconde as ações do rondel, mantém Desfazer/Refazer); header ganha botão "🏁 Placar final". `PlacarFinal` mostra ranking com vencedor destacado, detalhamento por jogador, nota "desempate pela [Nação] ([n] PP)", **Desfazer fim de jogo** (undo da tributação que encerrou indevidamente) e **Nova partida** (com confirmação → `App` descarta o save via `persistencia.descartarSave()` e chama `jogo.reiniciar()`, novo método de `useGame` que zera o estado e volta ao wizard). Histórico e Export/Import continuam acessíveis no pós-jogo. `TributacaoModal` não dispara mais o fim via callback (o Dashboard detecta); `FimDeJogoModal` placeholder da Sessão 4 removido. **Testes:** 77 no total (10 novos): `pontuacao.test.ts` (fim de jogo, cenário D detalhado, desempate em duas camadas — Índia empata/China decide — e empate real), `acoes.test.ts` (pular o Investidor com o jogo encerrado não investe nem passa a carta; parar segue funcionando), `PlacarFinal.test.tsx` (fim ponta a ponta com `useGame` real: placar abre a 25 PP, ações bloqueadas, undo fecha o placar e reabilita, nova partida confirma e chama o callback). `npm test`/`tsc -b`/`eslint`/`build` verdes. **Notas para a próxima sessão:** (1) o placar reabre via botão do header depois de fechado; se a partida for retomada de um save já com 25 PP, o placar abre automaticamente ao carregar; (2) a passagem de assentos/carta no caso "parar com jogo encerrado" não foi restringida — o manual só isenta o PULAR, então mantivemos o parar intacto. |
| 7 | PWA / suporte offline | ⬜ Pendente | Requer HTTPS, já garantido pelo GitHub Pages desde a Sessão 0. |
